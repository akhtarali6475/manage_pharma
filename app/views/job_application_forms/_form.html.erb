<%= form_with(model: job_application_form, local: true, class: 'form-horizontal center') do |form| %>
  <% if job_application_form.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(job_application_form.errors.count, "error") %> prohibited this job_application_form from being saved:</h2>

      <ul>
      <% job_application_form.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="form-group">
    <%= form.label :title, class: "col-md-4 control-label" %>
    <div class="col-md-6">
      <%= form.text_field :title, id: :job_application_form_title %>
    </div>
  </div>

  <div class="form-group">
    <%= form.label :description, class: "col-md-4 control-label"%>
    <div class="col-md-6">
      <%= form.text_area :description, id: :job_application_form_description %>
    </div>
  </div>

  <div class="form-group">
    <%= form.label :open, class: "col-md-4 control-label" %>
    <div class="col-md-6">
      <%= form.check_box  :open %>
    </div>
  </div>

  <div class="text-center">
    <%= form.submit "Submit", class: "btn btn-default btn-primary" %>
  </div>
<% end %>
